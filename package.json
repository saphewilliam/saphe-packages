{
  "name": "saphe-packages",
  "private": true,
  "version": "0.0.1",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/saphewilliam/saphe-packages.git"
  },
  "author": "saphewilliam <william@saphewebdesign.com>",
  "scripts": {
    "build": "rimraf ./packages/*/dist && tsc -b",
    "test": "jest",
    "lint": "eslint --ext .ts,.tsx .",
    "verify": "yarn build && yarn test && yarn lint",
    "changeset": "changeset",
    "release": "yarn verify && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "2.16.0",
    "jest": "26.6.3",
    "rimraf": "3.0.2",
    "ts-jest": "26.5.6",
    "tslib": "2.2.0",
    "typescript": "4.2.4"
  }
}
